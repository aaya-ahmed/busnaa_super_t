export type Ischool ={
    key: string;
}&ISchoolData;
export interface ISchoolData{
    address: string,
        countryCode: string,
        location?: string,
        logo?: string,
        name: string,
        phoneNumber: string,
}